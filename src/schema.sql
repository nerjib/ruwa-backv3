create table beneficiaries(
    id serial,
    first_name varchar,
    last_name varchar,
    family_name varchar,
    phone_no varchar,
    community varchar,
    ward varchar,
    package_type varchar,
    img_url varchar,
    gps varchar,
    occupation varchar,
    address varchar,
    remark varchar
);